# =============================================================================
# ECHOSUB BACKEND DEPENDENCIES - LIGHTWEIGHT VERSION
# 轻量级版本 - 仅外部API转录，无本地模型，无视频压制
# Optimized for low-resource cloud servers
# =============================================================================

# -----------------------------------------------------------------------------
# WEB FRAMEWORK & API
# -----------------------------------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.12

# -----------------------------------------------------------------------------
# DATABASE & ORM
# -----------------------------------------------------------------------------
sqlalchemy==2.0.35
alembic==1.16.0
psycopg2-binary==2.9.10
redis==5.0.8

# -----------------------------------------------------------------------------
# TASK QUEUE & BACKGROUND PROCESSING
# -----------------------------------------------------------------------------
celery==5.5.2
kombu==5.5.3
billiard==4.2.1
vine==5.1.0

# -----------------------------------------------------------------------------
# BASIC AUDIO PROCESSING (轻量级)
# -----------------------------------------------------------------------------
ffmpeg-python==0.2.0  # Only for basic audio extraction
pysrt==1.1.2  # Subtitle format support

# -----------------------------------------------------------------------------
# DATA PROCESSING & VALIDATION
# -----------------------------------------------------------------------------
pandas==2.2.3
numpy==1.26.4
pydantic==2.11.0
pydantic-settings==2.9.0

# -----------------------------------------------------------------------------
# AUTHENTICATION & SECURITY
# -----------------------------------------------------------------------------
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
bcrypt==4.3.0

# -----------------------------------------------------------------------------
# UTILITIES & HTTP
# -----------------------------------------------------------------------------
requests==2.32.0
httpx==0.28.0
python-dotenv==1.1.0
click==8.1.7
tqdm==4.67.0
rich==14.0.0

# -----------------------------------------------------------------------------
# TEXT PROCESSING (基本功能)
# -----------------------------------------------------------------------------
# spacy==3.8.0  # Disabled - not needed in lightweight mode

# -----------------------------------------------------------------------------
# DEVELOPMENT & TESTING (OPTIONAL)
# -----------------------------------------------------------------------------
pytest==8.3.0
pytest-cov==4.1.0
black==24.3.0
isort==5.13.0

# =============================================================================
# REMOVED DEPENDENCIES (不再需要的重型依赖)
# =============================================================================
# 
# REMOVED FOR LIGHTWEIGHT VERSION:
# - torch==2.1.1                    # 重型ML框架
# - torchaudio==2.1.1               # 音频ML处理
# - whisperx==3.1.1                 # 本地转录模型
# - faster-whisper==1.1.0           # 本地转录模型
# - transformers==4.30.2            # ML模型库
# - pyannote.audio==3.3.2           # 音频ML分析
# - librosa==0.10.2                 # 音频信号处理
# - soundfile==0.13.1               # 音频文件处理
# - openai==1.78.0                  # 可选，如果不使用OpenAI API
#
# =============================================================================
# INSTALLATION NOTES (轻量级版本)
# =============================================================================
# 
# 1. Install system dependencies:
#    - FFmpeg: apt-get install ffmpeg (仅用于基础音频提取)
#    - PostgreSQL: apt-get install postgresql-dev
#
# 2. Install basic Spacy model (轻量级):
#    python -m spacy download en_core_web_sm
#    python -m spacy download zh_core_web_sm
#
# 3. NO GPU/CUDA support needed (纯CPU运行)
#
# 4. For production deployment:
#    pip install gunicorn==21.2.0
#
# 5. Memory requirements: ~500MB RAM (vs ~4GB+ for full version)
#    CPU requirements: 1-2 cores sufficient
#
# =============================================================================